nelmio_api_doc:
    documentation:
        info:
            title: RoomCtrl API
            version: 1.0.0
            description: |
                This is the versioned API documentation for RoomCtrl.
                Current version: v1
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc$)

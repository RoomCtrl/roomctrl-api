services:
  # PHP-FPM 8.4 - Development
  roomctrl-php:
    container_name: roomctrl-php-dev
    build:
      context: .
      dockerfile: docker/php/Dockerfile.dev
    volumes:
      - .:/var/www/html
      - ./docker/php/custom.dev.ini:/usr/local/etc/php/conf.d/custom.ini
      # Cache Composer dependencies for better performance
      - composer-cache:/root/.composer/cache
    environment:
      APP_ENV: dev
      APP_DEBUG: 1
      XDEBUG_MODE: debug
      XDEBUG_CONFIG: client_host=host.docker.internal
    depends_on:
      - roomctrl-postgres
      - roomctrl-mailhog
    networks:
      - roomctrl-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # Nginx
  roomctrl-nginx:
    container_name: roomctrl-nginx-dev
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.dev.conf:/etc/nginx/conf.d/default.conf
      - ./docker/nginx/logs:/var/log/nginx
    depends_on:
      - roomctrl-php
    networks:
      - roomctrl-network

  # PostgreSQL
  roomctrl-postgres:
    container_name: roomctrl-postgres-dev
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-roomctrl_dev}
      POSTGRES_USER: ${POSTGRES_USER:-roomctrl}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-roomctrl123}
    ports:
      - "5432:5432"
    volumes:
      - roomctrl-postgres-data-dev:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d
    networks:
      - roomctrl-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-roomctrl}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # pgAdmin
  roomctrl-pgadmin:
    container_name: roomctrl-pgadmin-dev
    image: dpage/pgadmin4:latest
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@roomctrl.local}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin123}
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "5050:80"
    depends_on:
      - roomctrl-postgres
    volumes:
      - roomctrl-pgadmin-data-dev:/var/lib/pgadmin
    networks:
      - roomctrl-network

  # MailHog - Email testing
  roomctrl-mailhog:
    container_name: roomctrl-mailhog-dev
    image: mailhog/mailhog:latest
    ports:
      - "1025:1025"  # SMTP
      - "8025:8025"  # Web UI
    networks:
      - roomctrl-network

  # Redis - Cache & Sessions (optional)
  roomctrl-redis:
    container_name: roomctrl-redis-dev
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - roomctrl-redis-data-dev:/data
    networks:
      - roomctrl-network
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3

networks:
  roomctrl-network:
    driver: bridge

volumes:
  roomctrl-postgres-data-dev:
    driver: local
  roomctrl-pgadmin-data-dev:
    driver: local
  roomctrl-redis-data-dev:
    driver: local
  composer-cache:
    driver: local
